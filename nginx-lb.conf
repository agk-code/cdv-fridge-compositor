events {}
http {
    upstream backend {
        server nginx1;
        server nginx2;
    }

    server {
        listen 80;

        server_name: localhost;
        location / {
            proxy_pass http://backend;
        }
    }
}